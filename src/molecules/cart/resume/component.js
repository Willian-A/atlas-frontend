import styled from "styled-components";

const ResumeBox = styled.div`
  display: flex;
  position: relative;
  margin: 5px 0;
  border-top: 2px rgba(0, 0, 0, 0.2) solid;
  padding: 5px 0 0;

  h2.value {
    margin: 0 0 0 auto;
  }
`;

export { ResumeBox };
